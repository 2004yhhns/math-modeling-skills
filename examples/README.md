# Examples

This directory is for **demonstrations only**.

A real competition project should not be stored here by default.

Examples may show:

- an example `problem_brief.md`,
- a model-selection audit,
- baseline vs candidate experiments,
- a validation summary,
- how a skill updates a separate live workspace.

The first recommended demonstration is a public historical competition problem, not a live submission.
